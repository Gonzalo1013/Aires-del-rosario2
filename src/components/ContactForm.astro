<section id="contacto" class="bg-blue-100 py-16 px-6 md:px-12 lg:px-20">
  <div class="max-w-3xl mx-auto text-center">
    <h2 class="text-2xl sm:text-3xl md:text-4xl font-bold text-gray-800 mb-6">
      Contactanos
    </h2>
    <p class="text-gray-700 text-lg mb-8">
      ¿Necesitás un servicio urgente o querés hacer una consulta? Escribinos y te respondemos lo antes posible.
    </p>

    <!-- Formulario de contacto -->
    <form 
      class="grid gap-6 text-left"  
      action="https://formsubmit.co/airesderosario.service@gmail.com" 
      method="POST"
      >
      <input type="hidden" name="_captcha" value="true">
      <input type="hidden" name="_next" value="http://localhost:4321/gracias"/>
      <div>
        <label class="block text-gray-700 font-medium mb-2" for="nombre">Nombre</label>
        <input
          type="text"
          id="nombre"
          name="nombre"
          class="w-full p-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-400"
          placeholder="Tu nombre"
          required
        />
      </div>
      <div>
        <label class="block text-gray-700 font-medium mb-2" for="email">Email</label>
        <input
          type="email"
          id="email"
          name="email"
          required
          class="w-full p-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-400"
          placeholder="Tu email"
          required
        />
      </div>

      <div>
        <label class="block text-gray-700 font-medium mb-2" for="mensaje">Mensaje</label>
        <textarea
          id="mensaje"
          name="mensaje"
          rows="4"
          class="w-full p-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-400"
          placeholder="Escribí tu consulta acá..."
          required
          minlength="4"
        ></textarea>
      </div>

      <div class="text-center">
        <button
          type="submit"
          class="bg-black hover:bg-blue-800 text-white px-6 py-3 rounded-lg font-semibold cursor-pointer"
        >
          Enviar consulta
        </button>
      </div>
    </form>
  </div>
</section>
